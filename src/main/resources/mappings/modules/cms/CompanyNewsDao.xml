<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.cms.dao.CompanyNewsDao">
    
	<sql id="companyNewsColumns">
		a.id AS "id",
		a.title AS "title",
		a.content AS "content",
		a.news_number AS "newsNumber",
		a.news_date AS "newsDate",
		a.img AS "img",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="companyNewsJoins">
	</sql>
    
	<select id="get" resultType="CompanyNews">
		SELECT 
			<include refid="companyNewsColumns"/>
		FROM cms_company_news a
		<include refid="companyNewsJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="CompanyNews">
		SELECT 
			<include refid="companyNewsColumns"/>
		FROM cms_company_news a
		<include refid="companyNewsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="CompanyNews">
		SELECT 
			<include refid="companyNewsColumns"/>
		FROM cms_company_news a
		<include refid="companyNewsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cms_company_news(
			id,
			title,
			content,
			news_number,
			news_date,
			img,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{title},
			#{content},
			#{newsNumber},
			#{newsDate},
			#{img},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE cms_company_news SET 	
			title = #{title},
			content = #{content},
			news_number = #{newsNumber},
			news_date = #{newsDate},
			img = #{img},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE cms_company_news SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>