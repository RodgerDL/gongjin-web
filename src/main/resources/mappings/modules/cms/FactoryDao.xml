<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.cms.dao.FactoryDao">
    
	<sql id="factoryColumns">
		a.id AS "id",
		a.name AS "name",
		a.address AS "address",
		a.img AS "img",
		a.introduce AS "introduce",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="factoryJoins">
	</sql>
    
	<select id="get" resultType="Factory">
		SELECT 
			<include refid="factoryColumns"/>
		FROM cms_factory a
		<include refid="factoryJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Factory">
		SELECT 
			<include refid="factoryColumns"/>
		FROM cms_factory a
		<include refid="factoryJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="Factory">
		SELECT 
			<include refid="factoryColumns"/>
		FROM cms_factory a
		<include refid="factoryJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cms_factory(
			id,
			name,
			address,
			img,
			introduce,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{name},
			#{address},
			#{img},
			#{introduce},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE cms_factory SET 	
			name = #{name},
			address = #{address},
			img = #{img},
			introduce = #{introduce},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE cms_factory SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>