<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.product.dao.CmsProductKindDao">
    
	<sql id="cmsProductKindColumns">
		a.id AS "id",
		a.kd_name AS "kdName",
		a.kd_nm AS "kdNm",
		a.kd_order AS "kdOrder",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="cmsProductKindJoins">
	</sql>
    
	<select id="get" resultType="CmsProductKind">
		SELECT 
			<include refid="cmsProductKindColumns"/>
		FROM cms_product_kind a
		<include refid="cmsProductKindJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="CmsProductKind">
		SELECT 
			<include refid="cmsProductKindColumns"/>
		FROM cms_product_kind a
		<include refid="cmsProductKindJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="CmsProductKind">
		SELECT 
			<include refid="cmsProductKindColumns"/>
		FROM cms_product_kind a
		<include refid="cmsProductKindJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cms_product_kind(
			id,
			kd_name,
			kd_nm,
			kd_order,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{kdName},
			#{kdNm},
			#{kdOrder},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE cms_product_kind SET 	
			kd_name = #{kdName},
			kd_nm = #{kdNm},
			kd_order = #{kdOrder},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE cms_product_kind SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>